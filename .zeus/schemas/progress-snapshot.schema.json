{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Progress Snapshot",
  "description": "Per-task agent completion snapshot. Written by each agent on completion. Zeus reads at the start of each turn. See Part III §2 of cursor-kit-v2-FINAL.md.",
  "type": "object",
  "required": ["task_id", "agent", "progress_pct", "current_step", "artifacts", "gate_self_check", "updated_at"],
  "properties": {
    "task_id": {
      "type": "string",
      "pattern": "^T\\d+$"
    },
    "agent": {
      "type": "string",
      "description": "Agent that produced this snapshot."
    },
    "progress_pct": {
      "type": "integer",
      "minimum": 0,
      "maximum": 100,
      "description": "Completion percentage. 100 = done."
    },
    "current_step": {
      "type": "string",
      "description": "Human-readable description of current/final step."
    },
    "artifacts": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Absolute or project-relative paths to produced artifacts. Paths, not descriptions."
    },
    "gate_self_check": {
      "type": "string",
      "enum": ["PASS", "FAIL", "PARTIAL"],
      "description": "Agent's Tier 1 self-check result against its contract."
    },
    "notes": {
      "type": ["string", "null"],
      "description": "Flags for downstream agents. Mandatory when flagging utility classes, mock strategy needs, etc."
    },
    "updated_at": {
      "type": "string",
      "format": "date-time"
    }
  }
}
